/**
 * mapDispatchToProps
 * connect 函数的第二个参数，用来建立UI组件的参数到`store.dispatch`方法的映射
 * ---
 * actionCreater 格式参考
 */
// export default (params: object = {}) => (
//   dispatch: (arg: object) => void,
//   getState: () => object
// ) => {
//   dispatch(params);
// };
